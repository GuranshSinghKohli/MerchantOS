{
	admin off
	email {$ACME_EMAIL}
}

{$SITE_ADDRESS:http://:80} {
	encode gzip
	@api path /api/* /health /ready /ready/*
	handle @api {
		reverse_proxy 127.0.0.1:8000
	}
	handle {
		reverse_proxy 127.0.0.1:3000
	}
}
