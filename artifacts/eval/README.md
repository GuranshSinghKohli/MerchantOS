# AgentBench artifacts

- `baseline.json` — checked-in FakeLLM snapshot for Phase 11. Rates must stay 1.0 / 0 failures.
- `latest.json` — generated by `uv run python -m merchantos_agentbench.runner` (gitignored).
